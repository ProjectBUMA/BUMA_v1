<% include ../partials/header %>

    <% users.forEach(function(user){ %>
       <%if(!user.isManager){%>
            <div class="row">
              <%= user.username %> 
              <br>
                  <% for(var i =0; i< user.dayOff.length;i++){ %>
                      
                      <p> <%= user.dayOff[i].title%> 
                      <br><%=user.dayOff[i].start%> 
                      <br><%=user.dayOff[i].end%> 
                      <br><%=user.dayOff[i].message%> </p>
                      
                      <form action="/calendar/dayoffapprove/<%=user._id%>/<%=user.dayOff[i].calendarID%>?_method=DELETE" method="POST">
                          <button class="btn btn-success btn-large">Approve</button>
                      </form>
                      
                      <form action="/calendar/dayoffreject/<%=user._id%>/<%=user.dayOff[i].calendarID%>?_method=DELETE" method="POST">
                          <button class="btn btn-danger btn-large">Reject</button>
                      </form>
                  <% }%>
            </div>
        <% } %>
        <br>
     <% }) %>


<% include ../partials/footer %>